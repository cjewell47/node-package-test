{
  "name": "arrayfetch",
  "version": "1.0.0",
  "description": "package to fetch an array of urls containing JSON data",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 5000 --require @babel/register --exit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/register": "^7.12.10",
    "mocha": "^8.2.1"
  }
}
